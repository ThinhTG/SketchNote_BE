server:
  port: 8088

spring:
  application:
    name: interaction-service
  cloud:
    discovery:
      enabled: true
  datasource:
    url: jdbc:postgresql://139.59.119.65:5432/interactiondb
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_HOST:http://localhost:8761/eureka/}


