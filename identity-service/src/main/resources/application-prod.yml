server:
  port: 8089

app:
  ip: 139.59.119.65

spring:
  application:
    name: identity-service
  datasource:
    url: jdbc:postgresql://${app.ip}:5432/usersdb
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  security:

    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://${app.ip}:8090/realms/sketchnote

idp:
  url: ${KEYCLOAK_URL:http://${app.ip}:8090}
  client-id: sketchnote
  client-secret: QYHYNsUNOS40S0Z28N7IytIf9bLYhtK3

eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_HOST:http://${app.ip}:8761/eureka/}
  instance:
    prefer-ip-address: true
    ip-address: ${app.ip}
